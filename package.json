{
  "name": "api-labifmaker--json",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/server.ts",
    "start": "tsup src/server.ts --format cjs --dts-resolve --minify --clean --sourcemap --dts && node ./dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.21.0",
    "ts-node-dev": "^2.0.0",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "tsconfig-paths": "^4.2.0"
  }
}
